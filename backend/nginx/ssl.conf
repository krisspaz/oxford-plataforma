# SSL/TLS Configuration for Production including TLS 1.3 and HSTS

ssl_protocols TLSv1.2 TLSv1.3;
ssl_prefer_server_ciphers on;

# Modern cipher suite (Mozilla guidelines)
ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;

# Session caching
ssl_session_timeout 1d;
ssl_session_cache shared:SSL:50m;
ssl_session_tickets off;

# OCSP Stapling
ssl_stapling on;
ssl_stapling_verify on;
resolver 8.8.8.8 8.8.4.4 valid=300s;
resolver_timeout 5s;

# HSTS (Strict-Transport-Security) enabled in default.conf, but good to reinforce here if included separately
# add_header Strict-Transport-Security "max-age=63072000" always;
