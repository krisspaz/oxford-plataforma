lexik_jwt_authentication:
    secret_key: '%kernel.project_dir%/config/jwt/private.pem'
    public_key: '%kernel.project_dir%/config/jwt/public.pem'
    pass_phrase: '%env(JWT_PASSPHRASE)%'
    token_ttl: 604800 # 7 days (prevents frequent logouts)
    token_extractors:
        authorization_header:
            enabled: true
            prefix: Bearer
            name: Authorization
        cookie:
            enabled: true
            name: access_token

    # set_cookies:
    #     access_token:
    #         lifetime: null
    #         samesite: lax
    #         path: /
    #         domain: null
    #         secure: false
    #         httpOnly: true
